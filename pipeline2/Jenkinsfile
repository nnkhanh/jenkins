node {

	stage ('Apply') {
		echo projectdir
		dir(projectdir + '/dc11-dot-khanh-networking') {
			sh 'terraform init'
			sh 'terraform apply -auto-approve'
		}		
	}

	stage ('Destroy') {
		dir(projectdir + '/dc11-dot-khanh-networking') {
			sh 'terraform destroy -auto-approve'
		}		
	}
}
