node {

	stage ('Apply') {
		echo projectdir
		dir('dc11-dot-khanh-networking') {
			sh 'terraform init'
			sh 'terraform apply -auto-approve'
		}		
	}

	stage ('Destroy') {
		dir('dc11-dot-khanh-networking') {
			sh 'terraform destroy -auto-approve'
		}		
	}
}
